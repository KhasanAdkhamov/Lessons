Запдача:
Журнал действий с отменой:

Условие: Создай систему, которая записывает действия пользователя в LinkedList. Реализуй функции: добавить действие, показать все действия, отменить последнее действие (undo), повторить отменённое (redo).

Подсказка: Используй addLast() для добавления, removeLast() для отмены. Для redo — второй LinkedList.