/*
* ═══════════════════════════════════════════════════════════════════════
* ЗАДАНИЕ 1: Исправьте класс
* ═══════════════════════════════════════════════════════════════════════
*
* Класс Student ниже нарушает принцип инкапсуляции.
*
* Задачи:
* 1. Сделайте все поля private
* 2. Создайте геттеры для всех полей
* 3. Создайте сеттеры с валидацией:
*    - Имя не должно быть пустым
*    - Возраст от 16 до 100
*    - Средний балл от 0.0 до 5.0
* 4. Создайте метод displayInfo()
*/

    static class Student {
        public String name;
        public int age;
        public double averageGrade;
        
        public Student(String name, int age, double averageGrade) {
            this.name = name;
            this.age = age;
            this.averageGrade = averageGrade;
        }
        
        // TODO: Добавьте геттеры и сеттеры с валидацией
    }
    
    /*
     * ═══════════════════════════════════════════════════════════════════════
     * ЗАДАНИЕ 2: Класс "Товар"
     * ═══════════════════════════════════════════════════════════════════════
     * 
     * Создайте класс Product с инкапсуляцией:
     * 
     * Поля (все private):
     * - String name (название)
     * - double price (цена)
     * - int quantity (количество на складе)
     * 
     * Требования:
     * 1. Конструктор принимает все параметры
     * 2. Геттеры для всех полей
     * 3. Сеттеры с валидацией:
     *    - name: не пустое
     *    - price: больше 0
     *    - quantity: не отрицательное
     * 4. Метод getTotalValue() - возвращает общую стоимость (price * quantity)
     * 5. Метод sell(int amount) - уменьшает quantity, проверяя наличие товара
     * 6. Метод restock(int amount) - увеличивает quantity
     */
    
    static class Product {
        // TODO: Реализуйте класс
    }
    
    /*
     * ═══════════════════════════════════════════════════════════════════════
     * ЗАДАНИЕ 3: Класс "Пользователь" с паролем
     * ═══════════════════════════════════════════════════════════════════════
     * 
     * Создайте класс User с безопасным хранением пароля:
     * 
     * Поля:
     * - String username (логин)
     * - String password (пароль) - ТОЛЬКО SETTER, БЕЗ ГЕТТЕРА!
     * - boolean isActive (активен ли пользователь)
     * 
     * Методы:
     * 1. Конструктор принимает username и password
     * 2. getUsername() - геттер
     * 3. isActive() - геттер для boolean (обратите внимание на "is")
     * 4. setActive(boolean) - сеттер
     * 5. setPassword(String newPassword) - валидация: минимум 6 символов
     * 6. checkPassword(String password) - проверяет, совпадает ли пароль (возвращает boolean)
     * 
     * ВАЖНО: Пароль можно проверить, но нельзя прочитать напрямую!
     */
    
    static class User {
        // TODO: Реализуйте класс
    }
    
    /*
     * ═══════════════════════════════════════════════════════════════════════
     * ЗАДАНИЕ 4: Класс "Температура"
     * ═══════════════════════════════════════════════════════════════════════
     * 
     * Создайте класс Temperature, который хранит температуру в Цельсиях,
     * но может возвращать её в разных единицах:
     * 
     * Поля:
     * - double celsius (private)
     * 
     * Методы:
     * 1. Конструктор принимает температуру в Цельсиях
     * 2. getCelsius() - возвращает температуру в °C
     * 3. getFahrenheit() - ВЫЧИСЛЯЕМОЕ СВОЙСТВО: возвращает в °F (C * 9/5 + 32)
     * 4. getKelvin() - ВЫЧИСЛЯЕМОЕ СВОЙСТВО: возвращает в K (C + 273.15)
     * 5. setCelsius(double celsius) - валидация: не ниже абсолютного нуля (-273.15°C)
     * 6. setFahrenheit(double fahrenheit) - принимает °F, конвертирует и сохраняет в celsius
     * 
     * Обратите внимание: внутри хранится ТОЛЬКО celsius, остальное вычисляется!
     */
    
    static class Temperature {
        // TODO: Реализуйте класс
    }
    
    /*
     * ═══════════════════════════════════════════════════════════════════════
     * ЗАДАНИЕ 5: Класс "Прямоугольник" с валидацией
     * ═══════════════════════════════════════════════════════════════════════
     * 
     * Создайте класс Rectangle с инкапсуляцией:
     * 
     * Поля:
     * - double width (ширина)
     * - double height (высота)
     * 
     * Методы:
     * 1. Конструктор с двумя параметрами
     * 2. Конструктор для квадрата (один параметр - сторона)
     * 3. Геттеры и сеттеры с валидацией (значения > 0)
     * 4. getArea() - вычисляемое свойство: площадь
     * 5. getPerimeter() - вычисляемое свойство: периметр
     * 6. isSquare() - возвращает true, если это квадрат
     * 7. resize(double factor) - изменяет размеры в factor раз (1.5 = увеличить в 1.5 раза)
     */
    
    static class Rectangle {
        // TODO: Реализуйте класс
    }
    
    /*
     * ═══════════════════════════════════════════════════════════════════════
     * ЗАДАНИЕ 6: Класс "Email" с валидацией
     * ═══════════════════════════════════════════════════════════════════════
     * 
     * Создайте класс Email с тщательной валидацией:
     * 
     * Поля:
     * - String email (private)
     * 
     * Методы:
     * 1. Конструктор принимает email и валидирует его
     * 2. getEmail() - возвращает email
     * 3. setEmail(String email) - устанавливает email с валидацией
     * 4. getDomain() - вычисляемое свойство: возвращает домен (часть после @)
     * 5. getUsername() - вычисляемое свойство: возвращает имя пользователя (часть до @)
     * 6. isValid(String email) - СТАТИЧЕСКИЙ метод проверки email
     * 
     * Валидация email (упрощённая):
     * - Содержит ровно один символ @
     * - Есть символы до и после @
     * - После @ есть точка
     * - Нет пробелов
     * 
     * Примеры:
     * ✅ "user@example.com" - валидный
     * ✅ "test.user@mail.ru" - валидный
     * ❌ "invalid" - нет @
     * ❌ "@example.com" - нет имени пользователя
     * ❌ "user@" - нет домена
     * ❌ "user @example.com" - есть пробел
     */
    
    static class Email {
        // TODO: Реализуйте класс
    }
    
    /*
     * ═══════════════════════════════════════════════════════════════════════
     * ЗАДАНИЕ 7: Класс "Счётчик посещений"
     * ═══════════════════════════════════════════════════════════════════════
     * 
     * Создайте класс VisitCounter для подсчёта посещений веб-сайта:
     * 
     * Поля:
     * - int totalVisits (общее количество посещений)
     * - int todayVisits (посещений сегодня)
     * - final int MAX_DAILY_LIMIT (максимум посещений в день, например 1000)
     * 
     * Методы:
     * 1. Конструктор без параметров (инициализирует счётчики нулями, лимит - 1000)
     * 2. getTotalVisits() - только геттер, без сеттера!
     * 3. getTodayVisits() - только геттер
     * 4. registerVisit() - увеличивает оба счётчика на 1, но проверяет лимит
     * 5. resetDailyCounter() - сбрасывает todayVisits на 0
     * 6. hasReachedLimit() - проверяет, достигнут ли дневной лимит
     * 
     * Особенность: счётчики можно только увеличивать через registerVisit(),
     * напрямую установить нельзя!
     */
    
    static class VisitCounter {
        // TODO: Реализуйте класс
    }
    
    /*
     * ═══════════════════════════════════════════════════════════════════════
     * ЗАДАНИЕ 8: Класс "Дата рождения" с вычисляемым возрастом
     * ═══════════════════════════════════════════════════════════════════════
     * 
     * Создайте класс Person с датой рождения:
     * 
     * Поля:
     * - String name
     * - int birthYear (год рождения)
     * - int birthMonth (месяц: 1-12)
     * - int birthDay (день: 1-31)
     * 
     * Методы:
     * 1. Конструктор со всеми параметрами (с валидацией даты!)
     * 2. Геттеры для всех полей
     * 3. getAge() - ВЫЧИСЛЯЕМОЕ: возвращает возраст (2024 - birthYear)
     * 4. getBirthDate() - возвращает дату в формате "ДД.МЫ.ГГГГ"
     * 5. isAdult() - возвращает true, если возраст >= 18
     * 6. isMinor() - возвращает true, если возраст < 18
     * 
     * Валидация:
     * - birthYear: от 1900 до текущего года
     * - birthMonth: от 1 до 12
     * - birthDay: от 1 до 31 (упрощённо, без учёта месяцев)
     */
    
    static class Person {
        // TODO: Реализуйте класс
    }
    